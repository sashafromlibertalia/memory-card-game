import React from 'react';

const Footer = () => {
    return (
        <footer>
            <p style={{fontWeight: 500}}>I'm searching for the job ✨</p>
        </footer>
    );
};

export default Footer;